# Exam #1: "Last Race"

## Student: s353434 Jinghao Mei

## React Client Application Routes

- Route `/`: public instructions page. Anonymous visitors can only read the game instructions; logged-in users can move to the setup page.
- Route `/login`: login form with username and password fields.
- Route `/setup`: protected page showing the complete underground network map with stations, segments, and line legend; includes controls to start a new game and open the ranking.
- Route `/games/:gameId/planning`: protected planning page showing a shuffled stations-only map, the assigned start and destination stations, the list of all available segments, the selected route summary, and the 90-second countdown. Each segment can be selected once; route validity is checked by the server after submission.
- Route `/games/:gameId/execution`: protected page showing the valid submitted route execution one segment at a time, with the random event and updated coin total for each step. Invalid or incomplete routes skip this phase.
- Route `/games/:gameId/result`: protected page showing the final score and a control to start another game.
- Route `/ranking`: protected page showing the best score obtained by each registered user.
- Route `*`: fallback not-found page.

## API Server

- POST `/api/sessions`
  - Request body: `{ username, password }`.
  - Response: authenticated user object. Creates a Passport session cookie.
- GET `/api/sessions/current`
  - Request body: none.
  - Response: current authenticated user, or `401` if not logged in.
- DELETE `/api/sessions/current`
  - Request body: none.
  - Response: empty response after logout.
- GET `/api/network`
  - Request body: none. Protected endpoint.
  - Response: full network data for the setup page: stations, lines, and segments.
- POST `/api/games`
  - Request body: none. Protected endpoint.
  - Response: a new game with `gameId`, randomly assigned `startStation`, `destinationStation`, planning deadline, stations, and selectable segments. The destination is at least 3 segments away from the start.
- POST `/api/games/:gameId/submit`
  - Request body: `{ route: [{ segmentId }] }`. Protected endpoint.
  - Response: route validity, execution steps with random events and coin totals, and final score. The server rebuilds the submitted segment set into an ordered route from the assigned start to destination. Segment order and direction sent by the client are not trusted. Invalid or incomplete routes return score `0`. Repeated submissions for the same active game return the already computed result.
- GET `/api/games/:gameId/result`
  - Request body: none. Protected endpoint.
  - Response: final result of one game owned by the logged-in user. During an active browser session, this returns the in-memory result generated by route submission.
- GET `/api/ranking`
  - Request body: none. Protected endpoint.
  - Response: users ordered by their best score.

## Database Tables

- Table `users` - contains id, username, name, salt, password_hash.
- Table `stations` - contains id, name, is_interchange. Rendering coordinates are maintained by the React client layout adapter, not stored in the database.
- Table `lines` - contains id, name. No color is stored for a line.
- Table `segments` - contains id, line_id, station_a_id, station_b_id. Each segment connects two stations and belongs to exactly one line.
- Table `events` - contains id, description, effect. Effects are integer coin changes between -4 and +4.
- Table `games` - contains id, user_id, start_station_id, destination_station_id, score, play_at. Scores are stored as non-negative values.

## Main React Components

- `App` (in `client/src/App.jsx`): configures the MUI theme, authentication context, game context, lazy-loaded routes, and protected routes.
- `AppLayout` (in `client/src/pages/components/AppLayout.jsx`): shared page shell with navigation and logout controls.
- `SetupPage` (in `client/src/pages/SetupPage.jsx`): loads the full network, renders the setup map, and starts a new game.
- `PlanningPage` (in `client/src/pages/PlanningPage.jsx`): manages the timed planning phase, auto-submission on timeout, shuffled station layout, segment selection, and route submission.
- `ExecutionPage` (in `client/src/pages/ExecutionPage.jsx`): retrieves the submitted result and shows execution only for valid routes.
- `ResultPage` (in `client/src/pages/ResultPage.jsx`): shows the final score and invalid-route reason when applicable.
- `RankingPage` (in `client/src/pages/RankingPage.jsx`): loads and displays the best score ranking.
- `NetworkMap` (in `client/src/pages/components/NetworkMap.jsx`): SVG map renderer used by setup and planning with different visibility options.
- `SegmentPicker` (in `client/src/pages/components/SegmentPicker.jsx`): scrollable list of selectable station pairs; already selected segments are disabled.
- `RouteSummary` (in `client/src/pages/components/RouteSummary.jsx`): displays the route built by the player during planning.
- `ExecutionSteps` (in `client/src/pages/components/ExecutionSteps.jsx`): reveals route execution steps one at a time.
- `stationLayoutAdapter` (in `client/src/pages/adapters/stationLayoutAdapter.js`): applies front-end map coordinates to station DTOs and provides fallback positions for new stations.
- `useAuthSession` (in `client/src/pages/hooks/useAuthSession.js`): owns login, logout, and current-session loading state.
- `useGame` (in `client/src/pages/hooks/useGame.js`): owns network, current game, selected route, submission result, ranking, and related loading states.

## Screenshot

No static screenshot is committed. Run the client and server locally to inspect the current UI.

## Users Credentials

- username: alice, password: password, salt: alice-salt
- username: bruno, password: password, salt: bruno-salt
- username: carla, password: password, salt: carla-salt

## Use of AI Tools

AI tools were used to brainstorm the architecture, draft and review parts of the API/client implementation, and generate focused DAO/API/client tests. All generated output was manually adapted to the exam requirements and verified with the project test, lint, and build commands.
